# Splash assets

- **`splash-full.png`** — Native launch screen (portrait 1284×2778). Generated by `node scripts/generate-splash-full.js` from `icon.png` (radial green→black, grid, logo, wordmark, tagline). **`app.json` uses this file**; `splash.backgroundColor` is **`#050508`** and must match the image edges (no flash).
- **`splash-icon.png`** — Legacy / script output from `fix-splash-background.js` (optional).
- **`icon.png`** — App icon; source for the splash logo layer.

After changing the logo or splash design, run:

```bash
node scripts/generate-splash-full.js
```

Then **rebuild** the native app (`eas build`); OTA updates do not replace the native splash.
